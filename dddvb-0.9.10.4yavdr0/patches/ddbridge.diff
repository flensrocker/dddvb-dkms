diff -r 2cc595391006 a/ddbridge/ddbridge-core.c
--- a/ddbridge/ddbridge-core.c	Tue Nov 05 08:03:36 2013 +0100
+++ b/ddbridge/ddbridge-core.c	Tue Nov 05 09:18:10 2013 +0100
@@ -889,16 +889,19 @@
 	return status;
 }
 
-#ifdef CONFIG_DVB_DRXK
+#if IS_ENABLED(CONFIG_DVB_DRXK)
 static int demod_attach_drxk(struct ddb_input *input)
 {
 	struct i2c_adapter *i2c = &input->port->i2c->adap;
 	struct ddb_dvb *dvb = &input->port->dvb[input->nr & 1];
 	struct dvb_frontend *fe;
-
-	fe = dvb->fe = dvb_attach(drxk_attach,
-				  i2c, 0x29 + (input->nr&1),
-				  &dvb->fe2);
+	struct drxk_config config;
+
+	memset(&config, 0, sizeof(config));
+	config.adr = 0x29 + (input->nr & 1);
+	config.microcode_name = "drxk_a3.mc";
+
+	fe = dvb->fe = dvb_attach(drxk_attach, &config, i2c);
 	if (!fe) {
 		printk(KERN_ERR "No DRXK found!\n");
 		return -ENODEV;
@@ -1442,7 +1445,7 @@
 		if (tuner_attach_stv6110(input, 1) < 0)
 			return -ENODEV;
 		break;
-#ifdef CONFIG_DVB_DRXK
+#if IS_ENABLED(CONFIG_DVB_DRXK)
 	case DDB_TUNER_DVBCT_TR:
 		if (demod_attach_drxk(input) < 0)
 			return -ENODEV;
